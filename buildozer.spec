[app]
title = My App
package.name = myapp
package.domain = org.test
source.dir = .
source.include_exts = py,png,jpg,kv,atlas
requirements = python3, kivy, requests, Pillow, fastapi, uvicorn, torch, torchvision, numpy
android.permissions = INTERNET, CAMERA
version = 0.1

